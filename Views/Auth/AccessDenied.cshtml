@{
    ViewData["Title"] = "Access Denied";
}

<div class="auth-page">
    <section class="auth-panel hero-auth">
        <p class="eyebrow">RBAC</p>
        <h1>Access denied</h1>
        <p class="auth-subtitle">Your current role does not have permission to use that action.</p>
        <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Back to dashboard</a>
    </section>
</div>
